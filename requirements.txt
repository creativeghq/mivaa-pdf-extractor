# MIVAA PDF Extractor Dependencies
# Cloud-only architecture — GPU inference (SLIG visual embeddings + PaddleOCR-VL
# structural pass) runs on Modal; HuggingFace fully removed 2026-06-14.
# Vision is Anthropic-only (Claude Opus 4.7 via SDK >= 0.42, with Anthropic tool_use
# schema-locked output via emit_vision_analysis tool).
# OCR: Chandra v2 cloud endpoint only — retry-with-jitter (3 attempts at temps 0.0/0.4/0.8,
# widened from 0.0/0.1/0.2 on 2026-05-03 to break sticky-prose state on graphic-heavy pages)
# replaces the previous broken pytesseract/easyocr fallbacks (removed 2026-05-01).

# Core FastAPI and server dependencies
fastapi[standard]>=0.104.1
uvicorn[standard]>=0.24.0

# PDF processing dependencies
pymupdf4llm==0.0.12
PyMuPDF>=1.23.26
# pypdf REMOVED 2026-08-01 (audit #300). pip-audit reported 36 known vulnerabilities against
# the 4.3.1 this pin forced, and nothing in this repo imports it: its only consumer was
# llama-index-readers-file, which is no longer a dependency (uv.lock still lists it and is
# stale). Removing beats bumping — it drops 36 CVEs with zero API-compatibility risk in a
# library nobody calls. If a PDF-writing need returns, add it back on >=6.14.2,<7.0.0.
pandas==2.1.4

# pdfplumber REMOVED 2026-08-02. It backed TableExtractor.extract_tables_from_page,
# which no stage ever called (dead since #248) and which re-derived tables from the
# raw PDF — a bronze-layer read for something silver already holds. Tables now come
# from the PaddleOCR-VL Stage 1 structural pass (document_layout_analysis
# .layout_elements[].metadata.html), parsed by app/services/pdf/table_extraction.py.

# Essential scientific computing
numpy==1.26.4
scipy>=1.11.0

# Computer Vision libraries (cloud-only architecture - no local ML models)
scikit-learn>=1.5.0
opencv-python-headless>=4.8.0
# Pillow 10.4.0 carried 23 known vulnerabilities (audit #300); 12.3.0 clears all of them.
# Two major versions, so checked first: no use of anything removed in 11 or 12 (ANTIALIAS,
# ImageDraw.textsize, Image.LINEAR/CUBIC), and a full from-scratch pip resolve of this file
# lands 12.3.0 alongside opencv-python-headless, scikit-image, imageio and imagehash.
Pillow>=12.3.0,<13.0.0
scikit-image>=0.21.0
imageio>=2.31.0,<2.32.0
imagehash==4.3.1

# AI/ML API clients
# anthropic SDK dropped 2026-05-23 — standardized on httpx. The shim in
# ai_client_service._AnthropicShim* + claude_helper._call_anthropic_async/sync
# provides the same `.messages.create()` API without the SDK pin trap.
openai>=2.31.0
voyageai==0.3.7

# (huggingface-hub removed 2026-06-14 — SLIG moved to Modal; no app code imports it.
#  The Modal image installs its own huggingface_hub for the one-time weight pull.)

# Supabase integration
supabase>=2.28.3
postgrest>=2.28.3
gotrue>=2.12.4
storage3>=2.28.3
supafunc>=0.10.2
realtime>=2.28.3

# Vector store (pgvector via vecs)
vecs==0.4.5

# WebSocket support
websockets>=15.0,<16.0

# Configuration and environment management
python-dotenv>=1.2.2,<2.0.0  # 1.0.0 -> PYSEC-2026-2270 (audit #300)
pydantic>=2.8.0
# 2.0.0 floor admitted 2.14.1 (PYSEC advisory). Raised to the fixed release (audit #300).
pydantic-settings>=2.14.2; python_version>="3.8"

# HTTP client for external API calls
httpx>=0.28.0
requests>=2.32.2
aiohttp>=3.9.3

# Async support
aiofiles==23.2.1

# Monitoring
sentry-sdk[fastapi]>=2.35.0

# Security and validation
python-multipart>=0.0.6
email-validator>=2.1.0

# OCR: Chandra v2 only — see chandra_endpoint_manager.py. Pytesseract + EasyOCR
# removed 2026-05-01 (silent failure surfaces, see audit). Chandra retry handles it.

# NLP Dependencies
# 3.9.3 floor still admitted 3.9.4, which carries 4 advisories (audit #300, 2026-08-02).
nltk>=3.10.0

# Additional utilities
python-dateutil==2.8.2
psutil==5.9.6
PyJWT>=2.8.0

# Price scraping / competitor monitoring
price-parser>=0.3.4

# XML hardening — DTD/entity-expansion-safe RSS/Atom parsing (pentest #250 I1)
defusedxml>=0.7.1
