# MIVAA PDF Extractor Dependencies
# Cloud-only architecture - all ML inference via HuggingFace endpoints (SLIG, Qwen, OCR, YOLO)
# OCR: Chandra (cloud, primary) → EasyOCR (local fallback, lazy-loaded) → Pytesseract (last resort)

# Core FastAPI and server dependencies
fastapi[standard]>=0.104.1
uvicorn[standard]>=0.24.0

# PDF processing dependencies
pymupdf4llm==0.0.12
PyMuPDF>=1.23.26
pypdf>=4.0.1,<5.0.0
pandas==2.1.4

# Table extraction with pdfplumber
pdfplumber>=0.10.0

# Essential scientific computing
numpy==1.26.4
scipy>=1.11.0

# Computer Vision libraries (cloud-only architecture - no local ML models)
scikit-learn>=1.5.0
opencv-python-headless>=4.8.0
Pillow>=10.2.0,<11.0.0
scikit-image>=0.21.0
imageio>=2.31.0,<2.32.0
imagehash==4.3.1

# AI/ML API clients
openai>=2.31.0
anthropic>=0.94.0
voyageai==0.3.7

# HuggingFace Hub for cloud endpoint management (SLIG + Qwen endpoints)
huggingface-hub>=0.20.0

# Supabase integration
supabase>=2.28.3
postgrest>=2.28.3
gotrue>=2.12.4
storage3>=2.28.3
supafunc>=0.10.2
realtime>=2.28.3

# Vector store (pgvector via vecs)
vecs==0.4.5

# WebSocket support
websockets>=15.0,<16.0

# Configuration and environment management
python-dotenv==1.0.0
pydantic>=2.8.0
pydantic-settings>=2.0.0; python_version>="3.8"

# HTTP client for external API calls
httpx>=0.28.0
requests>=2.32.2
aiohttp>=3.9.3

# Async support
aiofiles==23.2.1

# Monitoring
sentry-sdk[fastapi]>=2.35.0

# Security and validation
python-multipart>=0.0.6
email-validator>=2.1.0

# OCR Dependencies (EasyOCR is lazy-loaded fallback — torch pulled in only when needed)
easyocr>=1.7.0
pytesseract>=0.3.10

# NLP Dependencies
nltk>=3.9.3

# Additional utilities
python-dateutil==2.8.2
psutil==5.9.6
PyJWT>=2.8.0

# Price scraping / competitor monitoring
price-parser>=0.3.4
