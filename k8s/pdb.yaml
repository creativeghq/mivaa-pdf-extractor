apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mivaa-pdf-extractor-pdb
  namespace: default
  labels:
    app: mivaa-pdf-extractor
spec:
  minAvailable: 1  # Always keep at least 1 pod running
  selector:
    matchLabels:
      app: mivaa-pdf-extractor

